#include "holberton.h"

/**
 * reverse_array - reverse content of an array.
 * @a: variable
 * @n: variable
 * Return: resulting string dest.
 */

void reverse_array(int *a, int n)
{
int i, tmp;


i = 0;
n = n - 1;
while (i <= 0)
{
tmp = a[i];
a[i] = a[n];
a[n] = tmp;
i++;
n--;
}
}
